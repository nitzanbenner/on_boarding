import React from 'react';
import LoginButton from '../LoginButton';

const Hero = () => {
    return(
        <div className="hero">
            <h1>Hello There.</h1>
            <LoginButton/>
        </div>
    )
}

export default Hero;